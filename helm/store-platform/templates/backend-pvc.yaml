apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-sqlite-data
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.backend.sqliteStorage }}
